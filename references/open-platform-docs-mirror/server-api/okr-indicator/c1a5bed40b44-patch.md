<!--
Mirrored official Feishu/Lark documentation page.
Source: https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/okr-v2/okr-indicator/patch
Markdown source: https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/okr-v2/okr-indicator/patch.md
Generated by scripts/crawl_client_docs.py.
-->
# 更新量化指标

更新现有量化指标的配置和数值，包括计算方式、状态、当前值、目标值和单位设置。

## 请求

基本 | &nbsp;
---|---
HTTP URL | https://open.feishu.cn/open-apis/okr/v2/indicators/:indicator_id
HTTP Method | PATCH
接口频率限制 | [100 次/分钟](https://open.feishu.cn/document/ukTMukTMukTM/uUzN04SN3QjL1cDN)
支持的应用类型 | Custom App
权限要求<br>**调用该 API 所需的权限。开启其中任意一项权限即可调用** | 更新 OKR 内容(okr:okr.content:writeonly)
字段权限要求 | **注意事项**：该接口返回体中存在下列敏感字段，仅当开启对应的权限后才会返回；如果无需获取这些字段，则不建议申请<br>获取用户 user ID(contact:user.employee_id:readonly)

### 请求头

名称 | 类型 | 必填 | 描述
---|---|---|---
Authorization | string | 是 | `tenant_access_token`<br>或<br>`user_access_token`<br>**值格式**："Bearer `access_token`"<br>**示例值**："Bearer u-7f1bcd13fc57d46bac21793a18e560"<br>[了解更多：如何选择与获取 access token](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-choose-which-type-of-token-to-use)
Content-Type | string | 是 | **固定值**："application/json; charset=utf-8"

### 路径参数

名称 | 类型 | 描述
---|---|---
indicator_id | string | 指标 ID。获取方式：调用「获取目标的量化指标」或「获取关键结果的量化指标」接口。<br>**示例值**："7342342398472398473"<br>**数据校验规则**：<br>- 长度范围：`1` ～ `20` 字符

### 查询参数

名称 | 类型 | 必填 | 描述
---|---|---|---
user_id_type | string | 否 | 用户 ID 类型<br>**示例值**：open_id<br>**可选值有**：<br>- open_id：标识一个用户在某个应用中的身份。同一个用户在不同应用中的 Open ID 不同。[了解更多：如何获取 Open ID](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-obtain-openid)<br>- union_id：标识一个用户在某个应用开发商下的身份。同一用户在同一开发商下的应用中的 Union ID 是相同的，在不同开发商下的应用中的 Union ID 是不同的。通过 Union ID，应用开发商可以把同个用户在多个应用中的身份关联起来。[了解更多：如何获取 Union ID？](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-obtain-union-id)<br>- user_id：标识一个用户在某个租户内的身份。同一个用户在租户 A 和租户 B 内的 User ID 是不同的。在同一个租户内，一个用户的 User ID 在所有应用（包括商店应用）中都保持一致。User ID 主要用于在不同的应用间打通用户数据。[了解更多：如何获取 User ID？](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-obtain-user-id)<br>**默认值**：`open_id`<br>**当值为 `user_id`，字段权限要求**：<br>获取用户 user ID(contact:user.employee_id:readonly)
department_id_type | string | 否 | 此次调用中使用的部门 ID 的类型<br>**示例值**：open_department_id<br>**可选值有**：<br>- department_id：以department_id来识别部门<br>- open_department_id：以open_department_id来识别部门<br>**默认值**：`open_department_id`

### 请求体

名称 | 类型 | 必填 | 描述
---|---|---|---
current_value_calculate_type | int | 否 | 指标的当前值的计算方式，目标仅支持 0、1；关键结果仅支持 0、2<br>**示例值**：0<br>**可选值有**：<br>- 0：手动更新<br>- 1：基于关键结果进度自动更新<br>- 2：基于拆解的关键结果进度更新
status_calculate_type | int | 否 | 指标的状态的计算方式，目标仅支持 0、1、2；关键结果仅支持 0、1<br>**示例值**：0<br>**可选值有**：<br>- 0：手动更新<br>- 1：基于进度和当前时间自动更新<br>- 2：基于风险最高的关键结果状态更新
start_value | number(float) | 否 | 指标的起始值，不支持目标修改<br>**示例值**：0<br>**数据校验规则**：<br>- 取值范围：`-99999999999` ～ `99999999999`
target_value | number(float) | 否 | 指标的目标值，不支持目标修改；不支持有承接记录的关键结果修改<br>**示例值**：0<br>**数据校验规则**：<br>- 取值范围：`-99999999999` ～ `99999999999`
current_value | number(float) | 否 | 指标的当前值，不支持基于关键结果进度自动更新的目标修改；不支持基于拆解记录自动更新的关键结果修改<br>**示例值**：0<br>**数据校验规则**：<br>- 取值范围：`-99999999999` ～ `99999999999`
unit | indicator_unit | 否 | 指标的单位，不支持目标修改；不支持有承接记录的关键结果修改
unit_type | int | 是 | 指标的单位类型<br>**示例值**：0<br>**可选值有**：<br>- 0：公共<br>- 1：自定义
unit_value | string | 是 | 指标单位的值。如果是自定义，字符长度不超过 5；如果单元类型是「公共」，单位值为以下枚举值 百分比：PERCENT；无单位：NONE；元：YUAN；美元：DOLLAR<br>**示例值**："PERCENT"<br>**数据校验规则**：<br>- 最小长度：`1` 字符
indicator_status | int | 否 | 指标的状态，只支持指标的状态的计算方式为手动更新时修改<br>**示例值**：0<br>**可选值有**：<br>- -1：未定义<br>- 0：正常<br>- 1：有风险<br>- 2：已延期

### 请求体示例
```json
{
    "current_value_calculate_type": 0,
    "status_calculate_type": 0,
    "start_value": 0,
    "target_value": 0,
    "current_value": 0,
    "unit": {
        "unit_type": 0,
        "unit_value": "PERCENT"
    },
    "indicator_status": 0
}
```

## 响应

### 响应体

名称 | 类型 | 描述
---|---|---
code | int | 错误码，非 0 表示失败
msg | string | 错误描述
data | \- | \-
indicator | indicator | 指标
id | string | 指标的 ID
create_time | string | 指标的创建时间，毫秒级时间戳
update_time | string | 指标的更新时间，毫秒级时间戳
owner | owner | 所有者
owner_type | string | 所有者类型<br>**可选值有**：<br>- user：员工
user_id | string | 员工 ID
entity_type | int | 指标所属的实体类型<br>**可选值有**：<br>- 2：目标<br>- 3：关键结果
entity_id | string | 指标所属的实体 ID
indicator_status | int | 指标的状态<br>**可选值有**：<br>- -1：未定义<br>- 0：正常<br>- 1：有风险<br>- 2：已延期
status_calculate_type | int | 指标的状态的计算方式<br>**可选值有**：<br>- 0：手动更新<br>- 1：基于进度和当前时间自动更新<br>- 2：基于风险最高的关键结果状态更新
start_value | number(float) | 指标的起始值
target_value | number(float) | 指标的目标值
current_value | number(float) | 指标的当前值
current_value_calculate_type | int | 指标的当前值的计算方<br>**可选值有**：<br>- 0：手动更新<br>- 1：基于关键结果进度自动更新<br>- 2：基于拆解的关键结果进度更新
unit | indicator_unit | 指标的单位
unit_type | int | 指标的单位类型<br>**可选值有**：<br>- 0：公共<br>- 1：自定义
unit_value | string | 指标单位的值。如果是自定义，字符长度不超过 5；如果单元类型是「公共」，单位值为以下枚举值 百分比：PERCENT；无单位：NONE；元：YUAN；美元：DOLLAR

### 响应体示例
```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "indicator": {
            "id": "7342342398472398473",
            "create_time": "1760604634563",
            "update_time": "1760604634563",
            "owner": {
                "owner_type": "user",
                "user_id": "ou_3bbe8a09c20e89cce9bff989ed840674"
            },
            "entity_type": 2,
            "entity_id": "7342342398472398473",
            "indicator_status": 0,
            "status_calculate_type": 0,
            "start_value": 0,
            "target_value": 0,
            "current_value": 0,
            "current_value_calculate_type": 0,
            "unit": {
                "unit_type": 0,
                "unit_value": "PERCENT"
            }
        }
    }
}
```

### 错误码

HTTP状态码 | 错误码 | 描述 | 排查建议
---|---|---|---
500 | 1009999 | Internal server error | 这是服务端内部错误，请稍后重试。如果问题持续存在，请联系飞书技术支持(https://applink.feishu.cn/TLJpeNdW)或您的客户成功经理。
500 | 1009998 | System exception | 发生了意外的系统异常，请重试请求。如果问题持续出现，请联系飞书技术支持(https://applink.feishu.cn/TLJpeNdW)或您的客户成功经理。
400 | 1001001 | Invalid parameters | 请对照 API 文档检查请求参数。常见问题包括：缺少必填参数、参数类型错误、或参数值不符合预期格式。
400 | 1001002 | No permission to access this OKR data | 此错误表示 OKR 系统内部权限限制（与应用权限无关）。请检查当前用户在 OKR 系统中是否有目标数据的阅览权限。
400 | 1001003 | User not found | 指定的用户不存在。请确认：1) 用户 ID 是否正确；2) 该用户是否属于当前租户；3) 是否使用了正确的 ID 类型。
400 | 1001004 | OKR data not found | 指定的 OKR 数据不存在。请确认：1) 传入的 ID 是否正确；2) 数据是否已被删除。
